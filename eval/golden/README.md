Golden cases live here: <case>/case.yaml (name, source, profile, tenant,
tasks, expectations: max_open_critical, max_open_high,
min_deliverable_types) + <case>/input/ mirroring project inputs.
Case #1 to add at office-merge time: the KVP STM defect project with its
corrected outputs. release_prompts must refuse when run_eval fails.
